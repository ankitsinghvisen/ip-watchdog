# -ip-watchdog
A Bash-based public IP monitor that continuously tracks your machine’s IP and sends automated email alerts on changes, with redundancy across multiple IP sources and systemd integration.
